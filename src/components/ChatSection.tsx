import { Section, ChatHeader, ChatContent, ChatFooter } from 'components';

export const ChatSection = () => {
  return (
    <Section>
      <ChatHeader />
      <ChatContent />
      <ChatFooter />
    </Section>
  );
};
